# Backend API URL
VITE_API_BASE_URL=http://localhost:3200

# Google OAuth Client ID (same one used in the backend .env as GOOGLE_CLIENT_ID)
# Get this from Google Cloud Console > APIs & Services > Credentials
# Leave blank to hide the Google sign-in button.
VITE_GOOGLE_CLIENT_ID=


